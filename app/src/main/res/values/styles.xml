<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="BaseTheme" parent="AppTheme">
    </style>
    <style name="welcomeTheme" parent="android:Theme.Light">
        <item name="android:windowBackground">@mipmap/welcome</item>
        <item name="android:windowNoTitle">true</item>
    </style>
    <style name="ActivtyBaseTheme" parent="BaseTheme">
        <item name="android:activityOpenEnterAnimation">@anim/right_in</item>
        <item name="android:activityOpenExitAnimation">@anim/left_out</item>
        <item name="android:activityCloseEnterAnimation">@anim/left_in</item>
        <item name="android:activityCloseExitAnimation">@anim/right_out</item>
    </style>
    <style name="ActivtyTheme" parent="BaseTheme">
        <item name="android:windowAnimationStyle">@style/ActivtyBaseTheme</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>
